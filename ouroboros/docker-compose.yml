version: '2'

services:
  ouroboros:
    image: pyouroboros/ouroboros:latest
    container_name: Ouroboros
    hostname: ouroboros
    restart: always
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - ../.env
      - ./.env